{
  "name": "discord-channel",
  "version": "0.1.0",
  "displayName": "Discord Channel",
  "description": "Discord bot integration over the raw Gateway WebSocket + REST API. Pair a DM, converse in servers, and receive results with approval buttons.",
  "icon": "MessagesSquare",
  "author": "PersonalClaw",
  "tags": [
    "channel",
    "bundled"
  ],
  "cli": {
    "setup": "cli_setup:run",
    "doctor": "cli_doctor:probe"
  },
  "loggerRoots": [
    "discord_runtime"
  ],
  "provider": {
    "type": "channel",
    "implementation": "discord_runtime.transport:create_provider",
    "capabilities": [
      "messaging",
      "reactions",
      "threads"
    ],
    "settingsSchema": {
      "type": "object",
      "properties": {
        "bot_token": {
          "type": "string",
          "default": "",
          "x-meta": {
            "label": "Bot Token",
            "help": "Discord bot token from the Developer Portal (Bot → Reset Token). Sent as 'Authorization: Bot <token>'.",
            "sensitive": true
          }
        },
        "application_id": {
          "type": "string",
          "default": "",
          "x-meta": {
            "label": "Application ID",
            "help": "The Discord application's ID (Developer Portal → General Information). Public, not a secret — used for the bot invite URL."
          }
        },
        "dm_activation": {
          "type": "string",
          "default": "always",
          "enum": [
            "always",
            "mention",
            "off"
          ],
          "x-meta": {
            "label": "DM Activation",
            "help": "Default activation mode for direct messages."
          }
        }
      }
    }
  }
}
