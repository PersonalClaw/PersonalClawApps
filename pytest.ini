# Anchors pytest's rootdir here, so ./conftest.py (a scratch PersonalClaw home for every test)
# loads however pytest is started: from this directory, from inside a bundle, or by core's
# quality verifier, which runs each bundle with the bundle as its working directory.
[pytest]
